class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
        listb=[]
       
        for i in range(n):
            listb.append(nums[i])
            listb.append(nums[n+i]) 
        return listb
